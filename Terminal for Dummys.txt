An Idiots Guide to use GitHub Repos

Navigation to folders:
cd means change directors e.g. cd Documents
ls means list files --> shows the contents of current folder

When in GitHub-Local repo:

git pull --all
git add -f --all
git commit -a  -m "Update"
git push --all origin
